import { StateUser } from "./InterfaceState";

export enum ErrosForm {
    ERROR_EMAIL = "user_email",
    ERROR_NAME = "user_name",
    ERROR_SURNAME = "user_surname",
}

interface TypeErrorsPossible extends StateUser {
    
}