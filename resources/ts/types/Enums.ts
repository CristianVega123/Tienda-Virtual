export enum RoleUser {
    ADMIN = "Admin",
    USER = "Customer"
}

export enum SectionAdminSideBar {
    DASHBOARD = "dashboard",
    USER = "user",
    PRODUCTS = "products"
}

export enum ModalAction {
    DELETE = "Delete",
    UPDATE = "Update",
    
}